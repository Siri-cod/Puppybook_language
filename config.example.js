/**
 * Copy this file to config.js and fill in your values.
 *
 * Everyone shares ONE Supabase database. Which entries you see is determined
 * by two passwords you enter when opening the app (not stored here).
 *
 * Do not commit config.js (it is in .gitignore).
 */
export default {
  /** Project URL only — no /rest/v1 at the end */
  supabaseUrl: 'https://YOUR_PROJECT.supabase.co',
  supabaseAnonKey: 'YOUR_ANON_PUBLIC_KEY',
};
